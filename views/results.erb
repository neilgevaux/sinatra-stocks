<table class='table table-hover table-striped'>
  <% Stocks.each do |id, text| %>
    <tr>
      <th><%= text %></th>
      <td><%= @companies[id] || 0 %>
      <td><%= '#' * (@companies[id] || 0) %></td>
    </tr>
  <% end %>
</table>
<p><a href='/'>Pick more stocks</a></p>